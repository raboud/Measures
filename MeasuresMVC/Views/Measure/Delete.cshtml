@using MeasuresMVC.Utils;

@model RandREng.MeasureDBEntity.Measure

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Measure</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Enterred)
        </dt>

        <dd>
            @Html.DisplayFor2(model => model.Enterred)
        </dd>

    </dl>
    @using (Html.BeginForm("Delete", "Measure", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
	{
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
	}
</div>
